namespace Ca.Domain.Modules.Startup.Entities;

public record SuperAdminSeedInfo
(
   string RoleName,
   string FirstName,
   string LastName,
   string Email,
   string UserName,
   string Password
);
